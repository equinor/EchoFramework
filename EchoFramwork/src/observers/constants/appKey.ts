export type AppKey = 'search' | 'pdf' | 'relatedTag' | 'camera' | 'history';

export const appKey = {
    search: 'search' as AppKey,
    pdf: 'pdf' as AppKey,
    relatedTag: 'relatedTag' as AppKey,
    camera: 'camera' as AppKey,
    history: 'history' as AppKey
};
